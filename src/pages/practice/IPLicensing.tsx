import React from 'react';
import DirectIPLicensing from './DirectIPLicensing';

const IPLicensing: React.FC = () => {
  // Just directly render the Direct component
  return <DirectIPLicensing />;
};

export default IPLicensing; 